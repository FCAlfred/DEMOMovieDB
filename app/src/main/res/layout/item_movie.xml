<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/item_movie_container_margin_end"
    android:layout_marginBottom="@dimen/item_movie_container_margin_bottom">

    <ImageView
        android:id="@+id/image_view_item_movie"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/item_movie_image_view_height"
        android:background="@drawable/image_background_no_corners"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="@mipmap/ic_launcher"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/text_view_item_title"
        style="@style/Titles"
        android:layout_width="0dp"
        android:ellipsize="end"
        android:gravity="center"
        android:singleLine="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/image_view_item_movie"
        app:layout_constraintStart_toStartOf="@id/image_view_item_movie"
        app:layout_constraintTop_toBottomOf="@id/image_view_item_movie" />

</androidx.constraintlayout.widget.ConstraintLayout>